export const PROMPT_VERSION = "ai-estimate-v1";

export const DEFAULT_ESTIMATE_MODEL = "gpt-4o";

export const MINIMUM_ENGAGEMENT_ZAR = 85_000;

export const CEILING_LIKELY_ZAR = 8_000_000;

export const MIN_TIMELINE_WEEKS = 1;

export const MAX_TIMELINE_WEEKS = 78;

/** Seeded examples are still in use until Good Code replaces them with live history. */
export const USING_SEEDED_EXAMPLES = true;
